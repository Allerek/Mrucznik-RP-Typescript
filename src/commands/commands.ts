/* The code is importing the `COLORS` enum from the `@/enums/colors` module, the `getMPlayer` function
from the `@/login/classes/player` module, and the `PlayerEvent` class from the `@infernus/core`
module. */
// import { COLORS } from "@/enums/colors";
// import { getMPlayer } from "@/login/classes/player";
// import { PlayerEvent } from "@infernus/core";

// PlayerEvent.onCommandError(({ player, next }) => {
//     const MPlayer = getMPlayer(player);
//     if(!MPlayer.loggedIn)
//     player.sendClientMessage(COLORS.RED, "[KOMENDA] Błąd komendy!");
//     next();
//     return true;
// })